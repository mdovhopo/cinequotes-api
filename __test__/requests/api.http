GET http://{{host}}/api/util/setup-firestore-demo-data

###

GET http://{{host}}/api/util/create-pubsub-topic

###


GET http://{{host}}/ping

> {%
 client.test("Test ping, must not fail.", function() {
        client.assert(response.status === 200, "Response status is not 200");
        client.assert('pong' in response.body, 'Response body does not have "pong" key');
    });
%}

###
GET http://{{host}}/api/v1/movies

> {%
 client.test("Default data includes 4 items", function() {
        client.assert(response.status === 200, "Response status is not 200");
        client.assert(Array.isArray(response.body), 'Response is not an array!');
        client.log(response.body);
        const invalidItem = response.body.find(function(item) {
          return 'id' in item && item.id &&
                 'title' in item && item.title
        });
        client.assert(!, 'Found invalid item in response array!')
    });
%}

###

GET http://{{host}}/api/v1/movies/P1Paxb0S8C37YHF30QM9/quotes?locale=FR

###

POST http://{{host}}/api/v1/quotes
Content-Type: application/json

{
  "movieTitle": "The Hitchhiker's Guide to the Galaxy",
  "actor": "Douglas Adams",
  "quote": "Don't Panic."
}

###


